-- NPC script setup

-- This needs to correspond to the order of the scripts in the character prefab
-- If you change the scripts, you should add them to the end here.

Scripts = {
	ENTITY_STATE_SCRIPT = 0,
	CHARACTER_SCRIPT = 1,
	NAVGATION_SCRIPT = 2,
	SENSORIUM_SCRIPT = 3,
	BEHAVIOUR_SCRIPT = 4
}